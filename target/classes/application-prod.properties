# Connexion PostgreSQL (Railway)
spring.datasource.url=jdbc:postgresql://postgres.railway.internal:5432/railway
spring.datasource.username=postgres
spring.datasource.password=PrKCbFFrBEAcmKhrSpypIgljdNFShnfV

# Hibernate & JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Connexion pool Hikari (ajustable)
spring.datasource.hikari.maximum-pool-size=5

# Port et CORS (si React, Vue ou autre Frontend)
server.port=${PORT:8080}
app.cors.allowed-origins=https://zenlife-gs2w.onrender.com
